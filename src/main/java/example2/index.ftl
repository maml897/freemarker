ftl开始处理
<#list map.values() as key>
${key}
</#list>
==============================================================================
${map.hello}===${map['hello']}
==============================================================================
${map.size()}===${map['size']()}
==============================================================================

只有下面才和altMap有关系
<#list map?keys as key>
${key}
</#list>
